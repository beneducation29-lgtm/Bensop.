import React,{useMemo,useState}from'react';import{Mic,Search,ChevronRight}from'lucide-react';import{speakingService}from'../services/speakingService';type P={language:'en'|'zh';onNavigate:(p:string)=>void};export function SpeakingLabPage({language,onNavigate}:P){const[q,setQ]=useState(''),[level,setLevel]=useState('ALL');const list=useMemo(()=>speakingService.getActivities(language,{level,search:q}),[language,level,q]);const levels=Array.from(new Set(speakingService.getAllActivities(language).map(x=>x.level)));return <div className="min-h-[70vh] bg-[#050505] py-12"><div className="mx-auto max-w-7xl px-5 sm:px-8"><div className="mb-10 flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="mb-3 flex gap-2 text-xs font-mono text-[#D9FF3F]"><Mic className="h-4 w-4"/>SPEAKING LAB</div><h1 className="text-4xl font-black sm:text-6xl">NÓI TỰ TIN <span className="text-[#D9FF3F]">MỖI NGÀY.</span></h1></div><div className="relative lg:w-80"><Search className="absolute left-3 top-3.5 h-4 w-4 text-[#666]"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm bài nói..." className="w-full border border-[#292929] bg-[#111] px-10 py-3 text-sm outline-none"/></div></div><div className="mb-8 flex flex-wrap gap-2"><button onClick={()=>setLevel('ALL')} className="rounded-full border border-[#D9FF3F] px-4 py-2 text-xs">TẤT CẢ</button>{levels.map(l=><button key={l} onClick={()=>setLevel(l)} className="rounded-full border border-[#292929] px-4 py-2 text-xs text-[#999]">{l}</button>)}</div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{list.map(a=><button key={a.id} onClick={()=>onNavigate('/'+(language==='en'?'tieng-anh':'tieng-trung')+'/speaking/'+a.slug)} className="group border border-[#202020] bg-[#0d0d0d] p-5 text-left hover:border-[#D9FF3F]"><span className="text-[10px] text-[#D9FF3F]">{a.level} · {a.type}</span><h2 className="mt-5 text-xl font-bold group-hover:text-[#D9FF3F]">{a.title}</h2><p className="mt-2 line-clamp-2 text-sm text-[#777]">{a.prompt}</p><div className="mt-6 flex justify-end text-xs font-bold">LUYỆN NÓI <ChevronRight className="h-4 w-4"/></div></button>)}</div></div></div>}